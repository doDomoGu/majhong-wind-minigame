# 微信云开发接入

四人同房、房间列表、断线重进都走微信云开发。`miniprogram/preview.html` 未配置环境时仍用本机房间。

## 你需要做的（一次）

1. 用微信开发者工具打开本目录，确认 AppID 是 `wx19b1f7d7af205c27`（已在 `project.config.json`）。
2. 点工具栏 **云开发**，开通环境（按量付费即可）。
3. 若还没有 `miniprogram/js/config.js`，先复制 `miniprogram/js/config.example.js` 为 `miniprogram/js/config.js`。把环境 ID 填进 `CLOUD_ENV`（这个文件已 gitignore，不会进仓库）。
4. 云开发控制台 → 数据库 → 新建集合 **`rooms`**、**`players`**。
5. 两个集合权限都设为：**所有用户可读，所有用户不可写**（写入只走云函数）。
6. 在开发者工具里对 `cloudfunctions/login`、`cloudfunctions/room` 分别右键 → **上传并部署：云端安装依赖**。
7. 编译运行。大厅右上角点头像可自行改昵称，会写入 `players`。

## 集合 `players`

| 字段 | 说明 |
| --- | --- |
| `_id` | 微信 `openid` |
| `name` | 用户在大厅自己填的昵称，最多 12 字 |
| `createdAt` / `updatedAt` | 时间戳 |

首次登录若不存在记录，会自动写成「玩家」+ openid 后 4 位。

## 集合 `rooms`

| 字段 | 说明 |
| --- | --- |
| `_id` | 四位房号 |
| `status` | `waiting` / `playing` / `finished` |
| `hostId` / `hostName` | 房主，id 为微信 `openid` |
| `players` | `{ id, name, avatar }[]` |
| `seats` | `{ E, S, W, N }`，未开局为 `null` |
| `game` | 对局状态，未开局为 `null`。含场风局数、本场、供托、亲家、四家点数、本局立直、结算单、局历史、终局 pt |
| `createdAt` / `updatedAt` | 时间戳 |

第一期只落**当前房间**。跨场历史是第二期。

## 云函数

| 函数 | 作用 |
| --- | --- |
| `login` | 拿到 `openid`，读写 `players`（登录 / 改昵称） |
| `room` | `list` / `mine` / `get` / `create` / `join` / `leave` / `fillBots`；对局：`riichi` / `cancelRiichi` / `startSettle` / `submitSettle` / `undoSettle`。改过云函数后需重新 **上传并部署** |

客户端对当前房间做 `watch`，其他人加入或满员开局会自动刷新。

## 本地预览

`CLOUD_ENV` 为空，或浏览器打开 `miniprogram/preview.html`：不初始化云，继续用内存房间和示例数据。
