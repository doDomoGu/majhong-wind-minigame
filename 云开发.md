# 微信云开发接入

四人同房、房间列表、断线重进都走微信云开发。`preview.html` 未配置环境时仍用本机房间。

## 你需要做的（一次）

1. 用微信开发者工具打开本目录，确认 AppID 是 `wx19b1f7d7af205c27`（已在 `project.config.json`）。
2. 点工具栏 **云开发**，开通环境（按量付费即可）。
3. 复制环境 ID，填进 `js/config.js` 的 `CLOUD_ENV`。
4. 云开发控制台 → 数据库 → 新建集合 **`rooms`**。
5. `rooms` 权限设为：**所有用户可读，所有用户不可写**（写入只走云函数）。
6. 在开发者工具里对 `cloudfunctions/login`、`cloudfunctions/room` 分别右键 → **上传并部署：云端安装依赖**。
7. 编译运行。进大厅应看到「已连接云开发」。

## 集合 `rooms`

| 字段 | 说明 |
| --- | --- |
| `_id` | 四位房号 |
| `status` | `waiting` / `playing` / `finished` |
| `hostId` / `hostName` | 房主，id 为微信 `openid` |
| `players` | `{ id, name, avatar }[]` |
| `seats` | `{ E, S, W, N }`，未开局为 `null` |
| `createdAt` / `updatedAt` | 时间戳 |

第一期只落**当前房间**。对局点数、本场、供托、结算单后续加在同一条房间文档上。跨场历史是第二期。

## 云函数

| 函数 | 作用 |
| --- | --- |
| `login` | 用云调用拿到 `openid`，作为玩家 ID |
| `room` | `list` / `mine` / `get` / `create` / `join` / `leave` / `fillBots` |

客户端对当前房间做 `watch`，其他人加入或满员开局会自动刷新。

## 本地预览

`CLOUD_ENV` 为空，或浏览器打开 `preview.html`：不初始化云，继续用内存房间和示例数据。
